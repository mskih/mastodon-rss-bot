services:
  mastodon-rss-bot:
    build: .
    container_name: mastodon-rss-bot
    restart: unless-stopped
    env_file: .env
    network_mode: "host"     # share host networking (VPN-friendly)
    environment:
      - TZ=${TIMEZONE:-Europe/Madrid}
    volumes:
      - ./data:/app/data
