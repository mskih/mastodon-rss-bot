services:
  mastodon-rss-bot:
    build: .
    container_name: mastodon-rss-bot
    restart: unless-stopped
    env_file: .env
    network_mode: "host"      # share host VPN/DNS/routes
    environment:
      - TZ=${TIMEZONE:-Europe/Madrid}
    volumes:
      - ./data:/app/data
